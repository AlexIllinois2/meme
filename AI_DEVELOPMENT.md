# 📦 Meme (Tauri2) — AI Refactor & Completion Spec

## 0. 任务类型（非常重要）

这是一个**已开发一半的项目重构任务，不是从零开发**。

AI 必须遵循：

```text
优先复用现有代码
→ 删除不符合需求的功能
→ 修复错误实现
→ 补全缺失功能
→ 统一架构
```

禁止行为：

* ❌ 不要整体重写项目
* ❌ 不要随意替换技术栈
* ❌ 不要忽略已有实现

允许行为：

* ✅ 重构文件结构
* ✅ 修改函数逻辑
* ✅ 重命名（必要时）
* ✅ 删除无用代码

---

## 1. 项目基础信息

### 1.1 原项目参考

* 原项目（Vue3 + Rust 分离架构）
  [https://gitee.com/v8307640632/meme](https://gitee.com/v8307640632/meme)

* 当前模板项目（必须基于此开发）
  [https://github.com/fridzema/oxide-dock](https://github.com/fridzema/oxide-dock)

---

### 1.2 技术栈（强约束）

#### 前端

* Vue 3
* Varlet UI（必须引入）
* Bun（运行时）

```bash
bun install
bun run dev
```

禁止：

* npm
* pnpm

---

#### 后端

* Tauri 2（Rust）
* SQLite

建议：

* `sqlx` 或 `rusqlite`

---

## 2. 重构策略（AI必须执行）

### 2.1 代码处理优先级

```text
1. 能复用 → 保留
2. 不符合需求 → 修改
3. 完全无关 → 删除
4. 缺失功能 → 新增
```

---

### 2.2 文件命名不一致处理

```text
IF 旧文件逻辑正确：
    → 保留文件
    → 在内部适配新需求
ELSE:
    → 重构或删除
```

---

### 2.3 前后端职责划分

#### 前端负责：

* UI
* 状态管理
* 用户交互

#### 后端负责：

* 文件系统操作
* SQLite
* 索引构建
* 搜索

---

## 3. 核心设计原则

### 3.1 数据权威

```text
文件系统 = 唯一真实数据源
SQLite = 可重建索引缓存
```

---

### 3.2 数据一致性

所有操作必须同步：

| 操作 | 文件系统 | 数据库 | keywords.toml |
| -- | ---- | --- | ------------- |
| 新增 | ✅    | ✅   | 可选            |
| 删除 | ✅    | ✅   | ✅             |
| 修改 | ✅    | ✅   | ✅             |

---

### 3.3 删除规则

```text
必须：
- 弹窗确认
- 级联删除
```

---

## 4. 初始化流程（必须实现）

### 4.1 启动逻辑

```pseudo
IF 未配置表情目录:
    打开目录选择器
```

---

### 4.2 初始化步骤

```pseudo
1. 创建 SQLite（app data 目录）

2. 扫描用户目录

IF 空目录:
    -> 初始化结构（可选）

ELSE:
    IF 存在 keywords.toml:
        -> 校验格式
        -> 加载关键词
        -> 构建索引
    ELSE:
        -> 弹窗：
           "是否生成关键词配置？"
        IF yes:
            -> 自动生成
            -> 建立索引
```

---

## 5. 目录结构（强约束）

```text
root/
├── mode/
│   ├── group/
│   │   ├── images...
│
├── keywords.toml
```

---

## 6. UI 结构（必须匹配）

### 6.1 顶部栏

组件：

* 搜索框
* 新增图片按钮
* 菜单按钮

---

### 6.2 菜单功能（必须存在）

```text
编辑(进入编辑模式)
添加模式
添加分组
关键词管理（当前分组）
拼音搜索开关
首字母搜索开关
刷新索引
设置
```

---

### 6.3 Tab 结构

#### 第二层：模式

* 横向滚动
* 持久化

📱 Mobile：

* QQ / 微信分享选择

---

#### 第三层：分组

* 根据搜索过滤
* 排序：share_count DESC

---

### 6.4 表情宫格

必须支持：

* 自适应 grid
* 缩放（滚轮 / 双指）
* GIF 支持

---

## 7. 核心功能（必须实现）

### 7.1 搜索

匹配：

```text
keyword
pinyin（可开关）
abbr（可开关）
```

---

### 7.2 点击行为

#### PC

```text
复制图片到剪贴板（含 GIF）
share_count++
```

#### Mobile

```text
分享到 QQ / 微信
share_count++
```

---

### 7.3 Ctrl + V（PC）

```pseudo
IF 粘贴：
    图片 or 图片路径

-> 保存到当前分组
-> 更新索引
```

---

### 7.4 新增图片

```text
选择文件 → 复制 → 更新 DB
```

---

### 7.5 编辑模式

支持：

* 多选
* 删除
* 移动
* 复制

---

## 8. 索引系统（关键）

### 8.1 刷新机制（手动）

```pseudo
扫描目录：

新增文件 -> 插入 DB
缺失文件 -> 删除 DB
```

---

### 8.2 不做的事情

```text
❌ 不监听文件系统变化
```

---

## 9. keywords.toml（必须支持）

### 9.1 格式

```toml
[
  groups_keywords
]

"笑" = ["哈哈", "鹅鹅鹅鹅", 233]

[
  keywords_pinyin
]

# 仅需要生成拼音的添加keywords_pinyin

"哈哈" = { pinyin = "haha", abbr = "hh" }
"鹅鹅鹅鹅" = { pinyin = "eeee", abbr = "eeee" }
```

---

### 9.2 自动生成

规则：

* 分组名按中英文逗号拆分
* 中文：

  * 转拼音
  * 转首字母
* 非中文：

  * 不生成keywords_pinyin

---

## 10. 设置页（必须实现）

包含：

```text
主题（跟随系统/深色/浅色）
修改表情目录
模式管理
分组管理
关键词管理
表情管理
生成 keywords.toml
```

---

## 11. 跨平台要求

### 11.1 路径处理

必须：

```text
使用 Tauri API 获取路径
禁止硬编码路径
```

---

### 11.2 剪贴板

* PC：必须支持 GIF
* Mobile：使用系统分享

---

## 12. AI 执行步骤（强制流程）

AI 必须按顺序执行：

```text
1. 扫描当前项目结构
2. 标记：
   - 可复用
   - 需修改
   - 可删除

3. 优先修复后端（索引 + 文件系统）
4. 再实现 UI
5. 最后接入交互
```

---

## 13. 输出要求（给 AI）

AI 在编码时必须：

* 不留 TODO

---

## ✅ 最终目标

```text
一个可运行的 Tauri2 应用：

- 可管理本地表情
- 可搜索（关键词/拼音/缩写）
- 可分享
- 可重建索引
```
