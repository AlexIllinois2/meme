name: Build & Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          cache: true
      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev librsvg2-dev patchelf libgtk-3-dev libayatana-appindicator3-dev
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"
      - name: Install frontend deps
        run: bun install
      - uses: tauri-apps/tauri-action@v1
        with:
          manifest_path: src-tauri/Cargo.toml
          args: --bundles deb,rpm,appimage
      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-artifacts
          path: src-tauri/target/release/bundle/**/*

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          cache: true
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"
      - name: Install frontend deps
        run: bun install
      - uses: tauri-apps/tauri-action@v1
        with:
          manifest_path: src-tauri/Cargo.toml
          args: --bundles nsis
      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-artifacts
          path: src-tauri/target/release/bundle/nsis/*.exe

  build-android:
    runs-on: ubuntu-latest
    env:
      ANDROID_HOME: ${{ github.workspace }}/android-sdk
      ANDROID_NDK_HOME: ${{ github.workspace }}/android-sdk/ndk/26.3.11579264
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          cache: true
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"
          key: android
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle
      - name: Cache Android SDK
        id: android-sdk-cache
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/android-sdk
          key: android-sdk-ndk-26.3.11579264-platform36-bt35
      - name: Setup Android SDK & NDK
        if: steps.android-sdk-cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p $ANDROID_HOME/cmdline-tools
          cd $ANDROID_HOME/cmdline-tools
          curl -sL -o cmdline-tools.zip https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          unzip -q cmdline-tools.zip
          mv cmdline-tools latest
          rm cmdline-tools.zip
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null 2>&1 || true
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager \
            "platform-tools" \
            "platforms;android-36" \
            "build-tools;35.0.0" \
            "ndk;26.3.11579264"
      - name: Remove cargo config (let Tauri regenerate)
        run: rm -f src-tauri/.cargo/config.toml
      - name: Create local.properties
        run: echo "sdk.dir=$ANDROID_HOME" > src-tauri/gen/android/local.properties
      - name: Generate debug keystore for CI signing
        run: |
          keytool -genkeypair \
            -v \
            -keystore src-tauri/gen/android/app/debug.keystore \
            -storepass android \
            -alias androiddebugkey \
            -keypass android \
            -dname "CN=Android Debug,O=Android,C=US" \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000
          cat > src-tauri/gen/android/app/keystore.properties << 'PROPS'
          tauri.android.storeFile=debug.keystore
          tauri.android.storeType=JKS
          tauri.android.storePassword=android
          tauri.android.keyAlias=androiddebugkey
          PROPS
      - name: Install frontend deps
        run: bun install
      - name: Build frontend
        run: bun run build
      - name: Build Android APK
        run: bunx tauri android build --apk
      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: src-tauri/gen/android/app/build/outputs/apk/**/*.apk
          if-no-files-found: error

  release:
    needs: [build-linux, build-windows, build-android]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist-artifacts
      - name: Display downloaded artifacts
        run: find dist-artifacts -type f | head -30
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist-artifacts/**/*
          overwrite_files: true
          fail_on_unmatched_files: false
          name: meme ${{ github.ref_name }}
          body: |
            ## 构建产物

            ### Linux x64
            - deb: Debian/Ubuntu 安装包
            - rpm: Fedora/RHEL 安装包
            - appimage: 通用 AppImage

            ### Windows x64
            - exe: Windows 安装程序

            ### Android arm64
            - apk: Android 安装包